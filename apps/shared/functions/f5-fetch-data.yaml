apiVersion: batch/v1
kind: CronJob
metadata:
  name: f5-fetch-data
  namespace: openfaas-fn
spec:
  schedule: "* * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: f5-fetch-data
            image: bsord/f5-fetch-data:1.0.2
            imagePullPolicy: IfNotPresent
            volumeMounts:
            - name: f5-secret
              mountPath: "/var/openfaas/secrets"
              readOnly: true
            env:
            - name: SUBREDDIT
              value: "superstonk"
          restartPolicy: Never
          volumes:
          - name: f5-secret
            secret:
              secretName: f5-mongouri