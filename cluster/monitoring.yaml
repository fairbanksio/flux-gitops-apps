apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: monitoring
  namespace: flux-system
spec:
  interval: 2m30s
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./monitoring
  prune: true
  validation: client
